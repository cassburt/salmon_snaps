var salStore = {
  location: 'Pioneer Square'
  hours: 8
  minCustPerHour: 17
  maxCustPerHour: 88
  avgCookiePerSale: 5.2
  var randomCookiesPerHour = {
  function getRandomIntInclusive(minCustPerHour, maxCustPerHour) {
    return Math.floor(Math.random() * (max -min +1)) + min;
    }
  }
};
